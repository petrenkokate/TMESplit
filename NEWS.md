# TMESplit 0.99.0

* Initial pre-release scaffold (Bioconductor convention).
* `TMESplitResult` S4 class defined.
* Basilisk environment targets `TMESplitpy` from GitHub `main`.
* `tmesplit()` generic + method skeletons (no algorithmic body yet).
