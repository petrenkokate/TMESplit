% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/plotting.R
\name{plotSampleHeatmap}
\alias{plotSampleHeatmap}
\alias{plotSampleHeatmap,TMESplitResult-method}
\title{Plot per-sample composition heatmap split by dominant program}
\usage{
plotSampleHeatmap(x, ...)

\S4method{plotSampleHeatmap}{TMESplitResult}(
  x,
  composition = NULL,
  annotations = NULL,
  annotation_colors = NULL,
  scale = TRUE,
  cluster_samples = FALSE,
  cluster_cell_types = FALSE,
  row_split = TRUE,
  show_sample_names = FALSE,
  ...
)
}
\arguments{
\item{x}{A \link{TMESplitResult}.}

\item{...}{Extra aesthetics.}

\item{composition}{Optional \verb{samples x cell_types} numeric matrix. If
\code{NULL} (default), uses the composition stored in the result metadata.}

\item{annotations}{Optional \code{data.frame} of per-sample annotations;
rownames must match sample labels in \code{H_fractions}. Columns become
top-annotation tracks.}

\item{annotation_colors}{Optional named list of color vectors for each
annotation column. Keys match columns of \code{annotations}; each value is a
named vector mapping category -> color.}

\item{scale}{Logical; z-score each cell type across samples. Default
\code{TRUE}.}

\item{cluster_samples, cluster_cell_types}{Logical; Ward-cluster within
each program slice. Defaults both \code{FALSE} — rows are sorted by
dominant program (argmax of normalized \verb{[W_shared | W_specific_by_group]})
and within each program by loading descending, producing a diagonal
layout. Set either to \code{TRUE} to opt into Ward clustering.}

\item{row_split}{Logical; split rows by each cell type's dominant program
(mirrors the column split on samples). Default \code{TRUE}. Ignored when
\code{cluster_cell_types = TRUE}.}

\item{show_sample_names}{Logical; label each sample on the x-axis.
Default \code{FALSE} (sample count is usually high).}
}
\value{
A \link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap} object (invisibly), drawn as a side
effect.
}
\description{
Samples (rows) x cell types (columns) composition heatmap, z-scored per
cell type, with samples grouped by their dominant program (hard
assignment from \code{H_fractions}).
}
\section{Methods (by class)}{
\itemize{
\item \code{plotSampleHeatmap(TMESplitResult)}: Method for \link{TMESplitResult}.

}}
\examples{
res <- new("TMESplitResult")
tryCatch(plotSampleHeatmap(res),
         error = function(e) message(conditionMessage(e)))
}
